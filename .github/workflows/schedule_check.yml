name: 抢票定时巡检

on:
  workflow_dispatch:  # 允许手动触发
  schedule:
    # 分钟设定为从 5 分开始，每 10 分钟一次 (5, 15, 25, 35, 45, 55)
    # 北京时间 06:05 - 07:55 (对应 UTC 22:05 - 23:55)
    - cron: '5-55/10 22-23 * * *'
    
    # 北京时间 08:05 - 23:55 (对应 UTC 00:05 - 15:55)
    - cron: '5-55/10 0-15 * * *'

jobs:
  check-spots:
    runs-on: ubuntu-latest
    timeout-minutes: 5 # 设置超时，防止卡死

    steps:
      - name: 拉取代码
        uses: actions/checkout@v4

      - name: 设置Python环境
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: 安装依赖
        run: |
          pip install -r requirements.txt
          playwright install chromium

      - name: 运行脚本
        # 👇👇👇 重点检查这里！👇👇👇
        # env 必须在 run 的同一级别，或者紧跟着 run
        env:
          MAIL_SENDER: ${{ secrets.MAIL_SENDER }}
          MAIL_PASSWORD: ${{ secrets.MAIL_PASSWORD }}
          MAIL_RECEIVER: ${{ secrets.MAIL_RECEIVER }}
        # 👆👆👆 这一块不能少，也不能缩进错误 👆👆👆
        run: python main.py
